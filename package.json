{
  "name": "wiki-mcp-api",
  "version": "0.1.0",
  "description": "MCP server per la wiki di dominio LLM Wiki",
  "type": "module",
  "bin": "dist/stdio.js",
  "scripts": {
    "build": "tsup src/stdio.ts --format esm --no-splitting --dts && node -e \"const fs=require('fs');const f='dist/stdio.js';fs.writeFileSync(f,'#!/usr/bin/env node\\n'+fs.readFileSync(f,'utf8'));\" && chmod +x dist/stdio.js",
    "start": "node dist/stdio.js",
    "dev": "tsx src/stdio.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@mastra/core": "^1.61.0",
    "@mastra/mcp": "^1.17.1",
    "dotenv": "^16.0.0",
    "gray-matter": "^4.0.3",
    "proper-lockfile": "^4.1.2",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@types/proper-lockfile": "^4.1.4",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.11"
  }
}
